<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PraktikumWireframe">

        <!-- Halaman Konfirmasi -->
        <activity
            android:name=".ConfirmActivity"
            android:exported="false" />

        <!-- Halaman Input Alamat -->
        <activity
            android:name=".AddressActivity"
            android:exported="false" />

        <!-- Halaman Ringkasan Order -->
        <activity
            android:name=".OrderReviewActivity"
            android:exported="false" />

        <!-- Halaman Home / Pilih Makanan -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Halaman Register -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Halaman Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Halaman Pilih Auth (Sign up / Login) -->
        <activity
            android:name=".AuthChoiceActivity"
            android:exported="false" />

        <!-- SPLASH jadi launcher utama -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>




        <!-- MainActivity tidak dipakai, boleh dihapus
             Kalau mau dipakai untuk testing, jangan kasih intent-filter MAIN -->
        <!--
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        -->
    </application>
</manifest>
